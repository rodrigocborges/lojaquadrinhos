@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<br />
<div class="container">

    <form asp-controller="Auth" asp-action="Login" method="POST" id="login">
        <div class="form-group">
            <label>E-mail</label>
            <input type="email" class="form-control" placeholder="Digite seu email" id="Email" name="Email">
        </div>
        <div class="form-group">
            <label>Senha</label>
            <input type="password" class="form-control" placeholder="Digite sua senha" id="Password" name="Password">
        </div>
        <button type="submit" class="btn btn-primary">Fazer Login</button>
    </form>

    <div id="message" class="alert alert-success" role="alert">
        This is a success alert—check it out!
    </div>

</div>

<script src="https://code.jquery.com/jquery-3.5.1.min.js" integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script>
<script type="text/javascript">
    $("#message").hide();
    @*$("#login").submit(function (e) {
        e.preventDefault();
        $("#message").show();
        $.ajax({
            type: 'post',
            dataType: 'json',
            contentType: 'application/json',
            url: 'http://localhost:50516/api/Auth/login',
            data: JSON.stringify({ "email": $("#Email").val(), "password": $("#Password").val() }),
            success: function (r) {
                console.log(r);
                //$("#message").html(r.responseText);
                //$("#message").removeClass("alert-danger");
                //$("#message").addClass("alert-success");

                if (r.status == 200) {
                    localStorage.setItem("loginToken", r.responseText);

                    location.href = "@Url.Action("Control", "Admin")";
                }

            },
            error: function (r) {
                console.log(r);
                if (r.status == 200) {
                    localStorage.setItem("loginToken", r.responseText);

                    location.href = "@Url.Action("Control", "Admin")";
                }
                //$("#message").html(r.responseText);
                //$("#message").removeClass("alert-success");
                //$("#message").addClass("alert-danger");

            }
        });

    });*@

</script>